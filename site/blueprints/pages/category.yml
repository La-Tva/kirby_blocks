title: Catégorie
image: false

columns:
  - width: 1/1
    sections:
      articles_section:
        label: Articles de la catégorie
        type: pages
        template: article
        info: "👍 {{ page.votes_oui.toInt }} | 👎 {{ page.votes_non.toInt }}"
        status: all

